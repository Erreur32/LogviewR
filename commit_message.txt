release: v0.8.35 — dependency cleanup + CodeQL fixes

## For users
- Zero vulnerabilities (npm audit + GitHub CodeQL + Dependabot)
- Lighter install: vitest removed (27 packages), unused deps cleaned

## Technical
- Tests migrated to Node.js built-in node:test (zero dependency)
- Removed brace-expansion, stale overrides; moved concurrently/cross-env to devDeps
- CodeQL: regex injection validation, rate limiters on all fail2ban routes,
  ReDoS-safe IPv6 regex, CORS default false, URL substring fix
- .gitignore: all AI/dev tool patterns consolidated
- GitHub: 15 SEO topics added, Projects tab disabled
