feat: shared IpModal + clickable IPs everywhere + UX fixes (v0.4.1)

## IpModal partagé
- Nouveau composant `src/pages/fail2ban/IpModal.tsx` (exports: IpModal, GeoInfo, toFlag, fmtBantime)
- En-tête enrichi : IP + badge récidiviste + géoloc (drapeau, ville, pays, org)
- Layout 2 colonnes : Statistiques (bans, jails, dates) | Whois (pays, ASN, ISP, organisation)
- Bouton "Bannir dans recidive" avec retour visuel succès/erreur
- Auto-fetch géoloc si non fournie par le contexte appelant
- Historique scrollable avec en-têtes sticky

## IPs cliquables
- TabTracker : utilise IpModal partagé (suppression du composant local)
- TabJailsEvents : IPs dans le tableau ouvrent la modale de détail
- JailCard (vue Cartes) : IPs bannies cliquables
- JailExpandedGrid (vue Tableau) : IPs dans "Bans < 5 min" et "IPs bannies actives" cliquables

## Corrections UX
- BanHistoryChart : suppression de overflow:hidden qui masquait le SVG
- BanHistoryChart : collapse uniquement via le bouton ▾/▸ (plus de clic accidentel sur l'en-tête)
- Scroll remis à 0 au changement d'onglet principal et de vue dans TabJails (graphe était au-dessus du viewport)
- Barres de recherche centrées dans tous les onglets (TabJails, TabJailsEvents, TabFiltres, TabActions, TabTracker, TabStats)
- Suppression overflow horizontal non voulu sur les tableaux Événements et Tableau
