release: v0.8.15

## For users
- Active ban counter now matches fail2ban exactly (unban sync every 30s, history preserved)
- Ban history chart: grouped side-by-side bars sorted largest first
- Y-axis shows real data max, no artificial rounding
- Attempt notifications: domain badge added, jail badge and "actives" text removed
- Top Domains counts unique banned IPs across all jails combined
- Config cards (Runtime / Database / Config files) collapse to uniform height, no ghost border

## Technical
- connection.ts: unban_at INTEGER migration + index + backfill expired bans on startup
- Fail2banPlugin: _syncUnbans() on each /status poll; active-ban queries use unban_at IS NULL
- BanHistoryChart: effectiveMax = raw max, grouped bars (groupW/subBarW), sortedJails desc
- TabStats: TopCard titleTooltip prop + domainTitleTooltip (F2bTooltip 360px cyan)
- TabConfig: conditional alignSelf + borderBottom per card for clean collapsed state
- Fail2banPage: trackerActive from /tracker on mount; jailDomainsRef lazy enrichment fetch
- TabTracker: onActiveChange prop bubbles activeCount to parent
- Header: domain badge (orange); notificationStore: domain field on AppNotification
