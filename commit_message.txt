feat: Fail2ban UI polish + bantime fix + hourly chart + map crash fix (v0.4.8)

## Pour les utilisateurs
- Header plus compact, badges Horloge/Utilisateur réduits
- Menu Fail2ban élargi (220px), flèche réduction toujours à droite
- Notifications de ban centrées en haut de l'écran
- Graphique 24h : axe X par heure (00h–23h), autres périodes adaptent les labels
- Légende graphique : masque réellement la série et recalcule l'échelle Y
- Badge bantime coloré selon la durée (vert/bleu/orange/rouge)
- Bantime exact pour recidive/apache-shellshock via socket fail2ban-client
- Sections Regex pliées par défaut + badge compteur dans les Paramètres
- Carte Leaflet : crash au premier chargement corrigé (React Strict Mode)

## Backend — Fail2ban
- Fail2banPlugin.ts : parseNum gère bantime négatif (-1 permanent), unité semaines
- Fail2banPlugin.ts : getJailParam fallback via socket pour bantime/findtime/maxretry manquants
- Fail2banClientExec.ts : nouvelle méthode getJailParam(jail, param)
- Fail2banSqliteReader.ts : granularité horaire (strftime('%H')) quand days=1

## Frontend — Fail2ban
- BanHistoryChart.tsx : isHourly prop, effectiveMax visible jails, labelCountForDays
- Fail2banPage.tsx : granularity state, toasts centre-haut, sidebar 220px
- TabJails.tsx : bantime badge coloré par durée
- TabMap.tsx : loadScript race condition fix (_loaded flag)

## Frontend — Global
- Header.tsx, Clock.tsx, UserMenu.tsx : padding/tailles réduits
- SettingsSection.tsx : props collapsible/defaultCollapsed/badge
- RegexManagementSection.tsx : collapsible par défaut + badge compteur
- README.md : simplifié, commande curl fail2ban en un bloc
