feat: update checker + default page fix + log cleanup (v0.4.2)

## Mises à jour automatiques
- server/routes/updates.ts : vérification GitHub Tags API + GHCR manifest HEAD (dockerReady)
- updateStore.ts : store Zustand avec checkForUpdates / loadConfig / setConfig
- SettingsPage : UpdateCheckSection (toggle + fréquence + statut version/docker)
- App.tsx : bandeau sticky dismissable (amber), dismissal par version en localStorage, polling configurable

## Page par défaut
- Ajout option "fail2ban" + sélecteur de tab (12 tabs) dans DefaultPageSection
- server/routes/system.ts : correction bug — defaultPage/defaultPluginId/defaultLogFile jamais écrits dans PUT
- Ajout defaultFail2banTab, validation VALID_PAGES
- App.tsx : navigation effective vers page par défaut au login
- Fail2banPage : prop initialTab
- Fix blink : display:none au lieu de && sur tab Général, cache module-level, spinner inline

## Interface administration
- Onglet "Notifications" renommé "Webhooks" (en.json + fr.json)
- Exporter : suppression cadre "Aperçu des logs" (stats fichiers/plugins/erreurs)

## Nettoyage logs serveur
- console.log → logger.debug (gated) dans BasePlugin, Fail2banPlugin, NotificationPlugin,
  AnalysisPlugin, HostSystemLogPlugin, fail2banSyncService, configService, routes/updates.ts
- console.error → logger.error partout, aucune donnée sensible dans les logs
