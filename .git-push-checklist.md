# Checklist avant push Git

## ✅ Vérifications effectuées

### Fichiers sensibles
- ✅ `.env` est dans `.gitignore`
- ✅ `commit-message.txt` est dans `.gitignore` (généré par script)
- ✅ `commit-message.txt.example` n'est PAS ignoré (peut être commité)
- ✅ Fichiers de configuration sensibles (`config/*.conf`) sont ignorés
- ✅ Base de données (`*.db`, `*.db-shm`, `*.db-wal`) sont ignorées
- ✅ Répertoire `data/` est ignoré

### Docker Compose
- ✅ `docker-compose.yml` (production) : 
  - Utilise image depuis registry (`ghcr.io/erreur32/LogviewR:latest`)
  - JWT_SECRET via variable d'environnement (pas de valeur par défaut)
  - Healthcheck configuré
  - Volumes correctement montés
  
- ✅ `docker-compose.local.yml` (build local) :
  - Build local avec Dockerfile
  - Valeur par défaut pour JWT_SECRET (dev uniquement)
  - Healthcheck configuré
  
- ✅ `docker-compose.dev.yml` (développement) :
  - Hot reload activé
  - Valeur par défaut pour JWT_SECRET (dev uniquement)
  - Montages spécifiques pour développement

### Documentation
- ✅ README.md mis à jour avec les différents docker-compose
- ✅ CHANGELOG.md à jour avec version 0.1.2
- ✅ Plans mis à jour dans `.cursor/plans/`

### Scripts
- ✅ `scripts/update-version.sh` fonctionnel
- ✅ `commit-message.txt.example` créé comme template

## 📝 Commandes Git recommandées

```bash
# Vérifier les fichiers qui seront commités
git status

# Ajouter tous les fichiers (sauf ceux dans .gitignore)
git add .

# Vérifier qu'aucun fichier sensible n'est inclus
git status

# Commit avec message
git commit -m "feat: Version 0.1.2 - Améliorations UI/UX

✨ Ajouté
- Badge taille fichier dans statistiques du tableau de logs
- Affichage compact des filtres (titre et badges sur une ligne)

🔧 Modifié
- Suppression du compteur dans le bouton 'Fichiers de logs' du header
- Suppression du label 'Statistiques :' dans le tableau de logs
- Amélioration de l'affichage des filtres avec séparateur '|'

📝 Documentation
- Mise à jour du README avec documentation des différents docker-compose
- Mise à jour du CHANGELOG avec version 0.1.2
- Mise à jour des plans de développement"

# Créer un tag
git tag -a v0.1.2 -m "Version 0.1.2 - Améliorations UI/UX"

# Push
git push origin main
git push origin v0.1.2
```

## ⚠️ Points d'attention

1. **JWT_SECRET** : Vérifier que le fichier `.env` n'est pas commité
2. **Base de données** : Vérifier que `data/dashboard.db` n'est pas commité
3. **Fichiers de config** : Vérifier que `config/logviewr.conf` n'est pas commité (seulement `.example`)

## 🔍 Vérification finale

```bash
# Vérifier les fichiers sensibles qui seraient commités
git ls-files | grep -E "\.env$|\.key$|\.pem$|secret|token|\.db$"

# Devrait retourner vide ou seulement des fichiers .example
```
